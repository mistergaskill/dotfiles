# shortcut to this dotfiles path is $ZSH
export ZSH=$HOME/.oh-my-zsh

# your project folder that we can `c [tab]` to
export PROJECTS=~/dev

# name of the oh_my_zsh theme to use
ZSH_THEME="blinks"

# load the git plugin
plugins=(git)

# not sure what this does
source $ZSH/oh-my-zsh.sh

